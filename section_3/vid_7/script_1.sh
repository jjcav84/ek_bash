#!/bin/bash

# The escape character is also known as backslash and for example if backslash is placed before a character it tells the interpreter to interpret the character before the backslash literally; useful for including double quotes in your output

echo "\"Linux is Awesome\""
